<h4>Synchronizations</h4>
<% @device.synchronizations.order("id asc").each do |synchronization| %>
  <div class="well">
    <button class="btn btn-primary btn-sm execute-synchronization" data-id="<%= synchronization.id %>"><%= synchronization.name %></button>
  </div>
<% end %>
