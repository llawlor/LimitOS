<div class="page-header">
  <h1>New Device</h1>
</div>

<% if params[:device_type] != 'arduino' %>
  You can add a new Raspberry Pi device via the following command:
  <br><br>
  <%= render 'shared/install_command' %>

  <br><br>
  If you're adding an Arduino connected via I<sup>2</sup>C, use the following form:
  <br><br>
<% end %>

<div class="row">
  <div class="col-xs-12 col-md-6">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h4>New Device</h4>
      </div>
      <div class="panel-body">
        <%= render :partial => 'form' %>
      </div>
    </div>
  </div>
</div>
