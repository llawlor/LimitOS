<%- model_class = Device -%>
<div class="page-header">
  <h1><%=t '.title', :default => [:'helpers.titles.new', 'New %{model}'], :model => model_class.model_name.human.titleize %></h1>
</div>
You can add a new Raspberry Pi device via the following command:
<br><br>
<%= render 'shared/install_command' %>

<br><br>
However, if you want to add a device manually, use the following form:
<br><br>
<%= render :partial => 'form' %>
